# 운영 환경 변수 설정 예시
# 실제 사용 시 .env 파일로 복사하여 사용
# 민감한 정보는 AWS Systems Manager Parameter Store 또는 Secrets Manager 사용 권장

# Spring Profile
SPRING_PROFILES_ACTIVE=prd

# Database Configuration (실제 값으로 교체 필요)
DB_HOST=${PRD_DB_HOST}
DB_PORT=3306

# User Service & Product Service (common_db)
COMMON_DB_NAME=${PRD_COMMON_DB_NAME}
COMMON_DB_USERNAME=${PRD_COMMON_DB_USERNAME}
COMMON_DB_PASSWORD=${PRD_COMMON_DB_PASSWORD}

# Coupon Service (coupon_db)
COUPON_DB_NAME=${PRD_COUPON_DB_NAME}
COUPON_DB_USERNAME=${PRD_COUPON_DB_USERNAME}
COUPON_DB_PASSWORD=${PRD_COUPON_DB_PASSWORD}

# JWT Configuration (실제 값으로 교체 필요)
JWT_SECRET=${PRD_JWT_SECRET}
JWT_VALIDITY=3600

# External Service URLs (실제 값으로 교체 필요)
PRODUCT_SERVICE_URL=${PRD_PRODUCT_SERVICE_URL}
COUPON_SERVICE_URL=${PRD_COUPON_SERVICE_URL}
USER_SERVICE_URL=${PRD_USER_SERVICE_URL}

# Logging Level
LOGGING_LEVEL_COM_ECOMMERCE=INFO

# Actuator (운영에서는 제한적으로 노출)
MANAGEMENT_ENDPOINTS_WEB_EXPOSURE_INCLUDE=health,metrics